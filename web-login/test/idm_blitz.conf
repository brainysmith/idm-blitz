main-conf {
    data-dir = "./web-login/target/scala-2.10/test-classes"

    logger {
        dir-of-logs = ${main-conf.data-dir}"/logs"

        levels {
            root = "DEBUG"
            core = "DEBUG"
            Wl = "TRACE"
        }
    }

    tests {
        dir-of-tests = ${main-conf.data-dir}
    }
}

wl-conf {
    application.langs = "en"

    authenticators {
        services>login>BasicAuthenticator {
            order = 7
        }
        services>login>LdapBasicAuthenticator {
            order = 3
            host = d00siadb01
            port = 1636
            ssl = true
            initialConnections = 1
            maxConnection = 2
            userDn = "uid=${USERNAME},cn=Users,dc=esia,DC=r-style,DC=com"
        }
        #dummy>BasicAuthenticator2 {order = 100}
        #dummy>BasicAuthenticator3 {}

    }
}