main-conf {
    data-dir = "./core/target/test-classes"

    logger {
        dir-of-logs = ${main-conf.data-dir}

        levels {
            core = "DEBUG"
            test = "DEBUG"
        }
    }
}

idm-conf {
    idm-home = ${main-conf.data-dir}"/idp"
}
